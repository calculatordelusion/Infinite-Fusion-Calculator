import { config } from "./site.config";
import {
  gameInfo,
  genderRatios,
  maxStats,
  shapeDescriptions,
} from "./game.config";

export { config, gameInfo, genderRatios, maxStats, shapeDescriptions };
